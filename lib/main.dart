import 'package:flutter/material.dart';

void main() {
  runApp(const ColorJumperApp());
}

/// A simple Flutter app that demonstrates a color jumper.
class ColorJumperApp extends StatelessWidget {
  const ColorJumperApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
